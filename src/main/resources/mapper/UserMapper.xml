<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ssafy.bokduck.mapper.UserMapper">
    <resultMap id="UserResultMap" type="ssafy.bokduck.dto.UserDto">
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="name"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insertUser" parameterType="ssafy.bokduck.dto.UserDto" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (email, password_hash, name, phone, status, created_at, updated_at)
        VALUES (#{email}, #{passwordHash}, #{nickname}, #{phone}, #{status}, NOW(), NOW())
    </insert>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>

    <update id="updateUser" parameterType="ssafy.bokduck.dto.UserDto">
        UPDATE users
        <set>
            <if test="nickname != null">name = #{nickname},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="passwordHash != null">password_hash = #{passwordHash},</if>
            updated_at = NOW()
        </set>
        WHERE user_id = #{userId}
    </update>

    <update id="deleteUser">
        UPDATE users SET status = 'WITHDRAWN', updated_at = NOW() WHERE user_id = #{userId}
    </update>
    
    <insert id="insertInterest" parameterType="ssafy.bokduck.dto.UserInterestDto">
        INSERT INTO user_interests (user_id, interest_type, value, created_at)
        VALUES (#{userId}, #{interestType}, #{value}, NOW())
    </insert>

    <select id="findInterestsByUserId" resultType="ssafy.bokduck.dto.UserInterestDto">
        SELECT * FROM user_interests WHERE user_id = #{userId}
    </select>

    <delete id="deleteInterestsByUserId">
        DELETE FROM user_interests WHERE user_id = #{userId}
    </delete>
</mapper>
